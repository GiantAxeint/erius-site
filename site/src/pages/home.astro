---
// /home — Erius 门户 dashboard（v4：以问候大卡"6"为枢纽的向心式布局）
// 视觉中心：问候大卡（更大、更方正、略偏左下，朝"最新文章"左下对角呼应）
// 围绕：社交胶囊(下方更近) + 时钟(右上对角，不与日历对齐) + 日历(顶对齐 6 中心) + 音乐(斜右下对角) + 左列 User/最新文章
import Layout from '../layouts/Dashboard.astro';
import UserCard from '../components/UserCard.astro';
import ContactBar from '../components/ContactBar.astro';
import GreetingCard from '../components/GreetingCard.astro';
import ClockCard from '../components/ClockCard.astro';
import CalendarCard from '../components/CalendarCard.astro';
import LatestPostsCard from '../components/LatestPostsCard.astro';
import MusicPlayerCard from '../components/MusicPlayerCard.astro';
import BackToTop from '../components/BackToTop.astro';
import { profile, menuItems, socialLinks } from '../data/profile';
---
<Layout title="Erius!">
  <div class="stage">
    <div class="dash">
      <div class="bubble cell left-top" style="--i:0">
        <UserCard menuItems={menuItems} />
      </div>
      <div class="bubble cell center-main" style="--i:1">
        <GreetingCard name={profile.name} intro={profile.intro} />
      </div>
      <div class="bubble cell right-top" style="--i:2">
        <ClockCard />
      </div>
      <div class="bubble cell left-mid" style="--i:3">
        <LatestPostsCard />
      </div>
      <div class="bubble cell right-mid" style="--i:4">
        <CalendarCard />
      </div>
      <div class="bubble cell center-foot" style="--i:5">
        <ContactBar links={socialLinks} />
      </div>
      <div class="bubble cell right-foot" style="--i:6">
        <MusicPlayerCard />
      </div>
    </div>
  </div>
  <BackToTop />
</Layout>

<style>
  .stage { max-width: 1280px; margin: 0 auto; }
  .dash {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-areas:
      "main"
      "foot"
      "left"
      "right";
    gap: 18px;
  }
  /* 桌面：以问候卡"6"为枢纽的向心式布局
     - 左列稍窄、给中央问候卡留出大空间
     - 右列用 3 个 cell 高度错位，让 Clock/Calendar/Music 分别在 6 的右上/中右/右下
     - 时钟与日历在不同 grid 行 + 不同左偏移，故意错开避免垂直对齐 */
  @media (min-width: 960px) {
    .dash {
      grid-template-columns: minmax(190px, 220px) minmax(400px, 440px) minmax(190px, 220px);
      grid-template-areas:
        "left-top    main     right-top"
        "left-mid    main     right-mid"
        "left-foot   foot     right-foot";
      align-items: start;
      column-gap: 14px;
      row-gap: 14px;
    }
    /* 左列（UserCard/LatestPosts）：与问候卡顶对齐，左列中段卡略下错落 */
    .left-top    { margin-top: 0; }
    .left-mid    { margin-top: 28px; }
    /* 右列：时钟贴 6 右上对角 + 不与日历对齐；日历顶对齐 6 中心 + 略左贴 6；音乐斜右下对角靠 */
    .right-top   { margin-top: 6px;  margin-left: -10px; }  /* 时钟：贴 6 右上角 */
    .right-mid   { margin-top: 0;    margin-left: -6px; }   /* 日历：顶对齐 6 中心，略左贴 6 */
    .right-foot  { margin-top: 10px; margin-left: -4px; }   /* 音乐：向 6 斜右下靠 */
  }

  .center-main { grid-area: main; align-self: center; justify-self: center; width: 100%; max-width: 440px; /* 略向左下偏移，朝左列'最新文章'卡的左下对角呼应 */ margin-left: -10px; margin-top: 14px; }
  .center-foot { grid-area: foot; justify-self: center; margin-top: -36px; }  /* 上移贴近问候卡 */
  .left-top    { grid-area: left-top; }
  .left-mid    { grid-area: left-mid; }
  .right-top   { grid-area: right-top; }
  .right-mid   { grid-area: right-mid; }
  .right-foot  { grid-area: right-foot; }

  /* ===== 气泡入场动画 ===== */
  .bubble {
    opacity: 0;
    transform: translateY(18px) scale(0.7);
    animation: bubbleIn 0.75s cubic-bezier(0.34, 1.56, 0.64, 1) both;
    animation-delay: calc(var(--i, 0) * 0.1s + 0.05s);
  }
  @keyframes bubbleIn {
    0%   { opacity: 0; transform: translateY(24px) scale(0.6); }
    60%  { opacity: 1; transform: translateY(-4px) scale(1.05); }
    100% { opacity: 1; transform: translateY(0)    scale(1); }
  }
  @media (prefers-reduced-motion: reduce) {
    .bubble { animation: none; opacity: 1; transform: none; }
  }
</style>